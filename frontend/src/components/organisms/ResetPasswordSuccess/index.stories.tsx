import ResetPasswordSuccess from ".";

export default {
  title: "organisms/ResetPasswordSuccess",
  component: ResetPasswordSuccess,
};

const Template = () => <ResetPasswordSuccess />;

export const Reset_Password_Success = Template.bind({});

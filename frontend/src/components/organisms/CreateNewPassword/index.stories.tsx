import CreateNewPassword from ".";

export default {
  title: "organisms/Create New Password",
  component: CreateNewPassword,
};

const Template = () => <CreateNewPassword />;

export const Create_New_Password = Template.bind({});
